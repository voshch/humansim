dt: 0.05
ticks: 1000
warmup: 50
rounds: 3

stages:
  - label: open space
    agents: [10, 50, 100, 200]
  - label: 10x10 maze
    agents: [10, 50, 100, 200]
    walls: maze(10)

reference:
  label: noop
  params_file: pkg://arena_humansim/config/default.yaml
  params:
    occlusion: noop

candidate:
  label: bitmap
  params_file: pkg://arena_humansim/config/default.yaml
  params:
    occlusion: bitmap
